# npm configuration for MemoApp Frontend
# Use legacy peer deps to handle Angular version conflicts
legacy-peer-deps=true

# Optional: Speed up installs
prefer-offline=true

# Suppress engine warnings for dependencies
engine-strict=false

# Suppress npm output messages
fund=false
audit=false
loglevel=warn